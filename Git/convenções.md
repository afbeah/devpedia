# Convenções

## Comandos úteis

- Para navegar por pastas pelo terminal usamos:
  
  ```bash
  cd <nome_da_pasta>
  ```

- Para voltar para a pasta anterior usamos:

  ```bash
  cd ..
  ```

- Para voltar a pasta pessoal usamos:

  ```bash
  cd ~
  ```
