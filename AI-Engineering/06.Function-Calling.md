# Function Calling

Function Calling é um recurso que permite que um Large Language Model (LLM) solicite a execução de funções externas para obter informações ou realizar ações.

Seu principal objetivo é conectar modelos de Inteligência Artificial com APIs, bancos de dados e sistemas externos.

Em outras palavras:

> Function Calling permite que um LLM deixe de apenas responder perguntas e passe a interagir com outros sistemas.

## O problema

Imagine um usuário perguntando:

```txt
Qual é a previsão do tempo para amanhã?
```

O LLM não possui acesso automático à internet.

Da mesma forma, ele também não consegue:

- consultar um banco de dados;
- enviar um e-mail;
- criar um pedido;
- realizar um pagamento;
- buscar informações em uma API.

Ele apenas gera texto.

## Como funciona?

O Function Calling adiciona uma nova etapa ao fluxo.

```txt
Usuário
↓
LLM
↓
Função
↓
Sistema Externo
↓
Resultado
↓
LLM
↓
Resposta
```

O modelo identifica que precisa de uma informação externa.

Em vez de inventar uma resposta, ele solicita que uma função seja executada.

## Exemplo

Pergunta:

```txt
Qual é a temperatura em São Paulo?
```

Fluxo:

```txt
Usuário
↓
LLM
↓
weather()
↓
API de Clima
↓
24°C
↓
LLM
↓
Hoje a temperatura em São Paulo é de 24°C.
```

Perceba que o LLM não consultou diretamente a API.

Ele apenas decidiu qual função deveria ser executada.

## O papel do Desenvolvedor

O desenvolvedor define quais funções estarão disponíveis para o modelo.

Exemplo:

```txt
getWeather()

createOrder()

searchCustomer()

sendEmail()
```

O LLM escolhe quando utilizá-las.

A implementação da função continua sendo responsabilidade da aplicação.

## Exemplo em uma Aplicação

Imagine um sistema de e-commerce.

Pergunta:

```txt
Onde está meu pedido?
```

Fluxo:

```txt
Usuário
↓
LLM
↓
getOrderStatus()
↓
Banco de Dados
↓
Pedido enviado
↓
LLM
↓
Seu pedido foi enviado e está em transporte.
```

Sem Function Calling o modelo precisaria inventar uma resposta.

## Quando utilizar?

Function Calling costuma ser utilizado quando o modelo precisa:

- consultar APIs;
- acessar bancos de dados;
- executar operações;
- enviar notificações;
- consultar serviços externos;
- realizar automações.

## Benefícios

### Informações atualizadas

O modelo passa a utilizar dados em tempo real.

### Integração

Permite comunicação com diversos sistemas.

### Automação

O LLM deixa de apenas responder e passa a executar ações.

### Redução de alucinações

Em vez de inventar informações, o modelo consulta a fonte correta.

## Limitações

### Dependência das funções

O LLM só consegue utilizar funções previamente disponibilizadas.

### Segurança

As funções devem possuir controle de permissões.

### Latência

Cada chamada externa aumenta o tempo de resposta.

## Exemplos de uso

Function Calling é utilizado em:

- ChatGPT;
- Copilots;
- Assistentes Virtuais;
- Sistemas Corporativos;
- Agentes de IA.

## Relação com RAG

Embora pareçam semelhantes, possuem objetivos diferentes.

| RAG | Function Calling |
|------|------------------|
| Recupera documentos | Executa funções |
| Busca conhecimento | Executa ações |
| Utiliza Embeddings | Utiliza APIs e sistemas |
| Responde utilizando contexto | Responde utilizando resultados externos |

## Relação com outros conceitos

Fluxo comum:

```txt
LLM
↓
Prompt Engineering
↓
RAG
↓
Function Calling
↓
Sistema Externo
↓
Resposta
```

Enquanto o RAG fornece conhecimento ao modelo, o Function Calling permite que ele interaja com o mundo externo.

## Relação com o DevPedia

Function Calling conecta diversos conceitos da trilha de AI Engineering.

```txt
LLM
↓
Prompt Engineering
↓
RAG
↓
Function Calling
↓
AI Agents
↓
MCP
```

Ele representa a ponte entre modelos de linguagem e aplicações reais.

## Resumo

Function Calling é um mecanismo que permite que um Large Language Model solicite a execução de funções externas para consultar informações ou realizar ações.

Esse recurso conecta o modelo a APIs, bancos de dados e outros sistemas, tornando possível construir aplicações de Inteligência Artificial capazes de interagir com o mundo real.
