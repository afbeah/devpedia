# Cache

Cache é uma técnica utilizada para armazenar temporariamente dados acessados com frequência.

Seu objetivo é reduzir o tempo de resposta das aplicações e diminuir a carga sobre sistemas mais lentos, como bancos de dados e APIs externas.

Em outras palavras:

> Cache evita realizar o mesmo trabalho repetidamente.

## O problema

Imagine uma aplicação consultando o banco de dados a cada requisição.

Fluxo:

```txt
Usuário
↓
API
↓
Banco de Dados
↓
Resposta
```

Se milhares de usuários realizarem a mesma consulta, o banco precisará processar a mesma informação repetidamente.

Isso gera:

* maior consumo de recursos;
* aumento da latência;
* redução de performance.

## Como funciona?

O cache armazena o resultado de consultas já realizadas.

Fluxo:

```txt
Usuário
↓
API
↓
Cache
↓
Resposta
```

Se a informação estiver disponível no cache:

```txt
Cache Hit
```

A resposta é retornada imediatamente.

Se não estiver:

```txt
Cache Miss
↓
Banco de Dados
↓
Cache
↓
Resposta
```

A informação é buscada no banco e armazenada para futuras consultas.

## Exemplo prático

Imagine uma página de produtos.

Consulta:

```txt
Listar produtos mais vendidos
```

Sem cache:

```txt
Usuário
↓
Banco
↓
Resposta
```

Com cache:

```txt
Usuário
↓
Cache
↓
Resposta
```

A consulta torna-se muito mais rápida.

## Tipos de Cache

### Cache de Aplicação

Mantido pela própria aplicação.

Exemplo:

```txt
Memória RAM
```

### Cache Distribuído

Compartilhado entre múltiplas aplicações.

Exemplo:

```txt
Redis
```

### Cache de Navegador

Armazenado no navegador do usuário.

Exemplo:

```txt
Imagens

CSS

JavaScript
```

### CDN Cache

Distribuído geograficamente.

Exemplo:

```txt
Cloudflare

Akamai
```

## Benefícios

### Performance

Menor tempo de resposta.

### Escalabilidade

Redução da carga em bancos de dados.

### Economia

Menor consumo de recursos computacionais.

### Melhor experiência do usuário

Páginas carregam mais rapidamente.

## Desafios

### Dados desatualizados

O cache pode conter informações antigas.

### Invalidação

Decidir quando remover dados do cache.

### Consumo de memória

O cache também ocupa recursos.

## Cache e Microsserviços

Em arquiteturas distribuídas é comum:

```txt
API
↓
Cache
↓
Banco
```

Isso reduz a quantidade de consultas realizadas pelos serviços.

## Cache e Redis

Redis é uma das ferramentas mais utilizadas para implementação de cache.

Fluxo:

```txt
Aplicação
↓
Redis
↓
Banco
```

Por isso Redis costuma ser o próximo assunto após Cache.

## Quando utilizar?

Cache costuma ser útil para:

* consultas frequentes;
* dashboards;
* catálogos de produtos;
* configurações;
* dados pouco alterados.

## Quando evitar?

Informações extremamente sensíveis ou que mudam constantemente podem não se beneficiar de cache.

## Resumo

Cache é uma técnica utilizada para armazenar temporariamente dados acessados com frequência.

Seu principal objetivo é melhorar desempenho, reduzir carga sobre sistemas mais lentos e aumentar a escalabilidade das aplicações.
